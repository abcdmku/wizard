# React Integration

The `@wizard/react` package provides React hooks and components for building type-safe multi-step wizards with full TypeScript support.

## Installation

```bash
npm install @wizard/react @wizard/core
# or
pnpm add @wizard/react @wizard/core
# or
yarn add @wizard/react @wizard/core
```

## Quick Start

Here's an example of setting up a wizard in React.

```tsx
import { createWizardFactory } from '@wizard/core';
import { WizardProvider, useWizard } from '@wizard/react';

// 1. Define your wizard
const { defineSteps, step, createWizard } = createWizardFactory();

const steps = defineSteps({
  welcome: step({
    data: {} as { name: string },
    next: ['preferences'],
  }),
  preferences: step({
    data: {} as { theme: string },
    next: ['complete'],
  }),
  complete: step({
    data: {} as {},
    next: [],
  }),
});

export const MyWizard = createWizard(steps);

// 2. Wrap your app (or wizard container) with WizardProvider
function App() {
  return (
    <WizardProvider wizard={MyWizard}>
      <WizardFlow />
    </WizardProvider>
  );
}

// 3. Use the wizard in your components
function WizardFlow() {
  // Use the kitchen-sink hook (pass wizard instance)
  const { step, next, back } = useWizard(MyWizard);

  return (
    <div>
      <h1>Current Step: {step}</h1>
      <button onClick={back}>Back</button>
      <button onClick={next}>Next</button>
    </div>
  );
}
```

## Core Concepts

### Provider

The `WizardProvider` component makes your wizard available to all child components via React Context.

```tsx
<WizardProvider wizard={MyWizard}>
  <YourApp />
</WizardProvider>
```

### Hooks

`@wizard/react` provides several hooks to interact with your wizard.

- **`useWizard`**: Kitchen-sink hook for full access.
- **`useCurrentStep`**: Get the current step ID.
- **`useWizardStep`**: Get data and methods for a specific step.
- **`useWizardActions`**: Get stable navigation actions (no re-renders).
- **`useWizardSelector`**: Select specific state for performance.

> [!TIP]
> See the [API Reference](/typedoc/react) for full details on all available hooks.

## Best Practice: Typed Hooks

While you can use the hooks directly with type parameters (e.g., `useWizardStep<typeof MyWizard>('step')`), we recommend creating typed convenience hooks for your specific wizard.

```tsx
// wizard.ts
import { createWizardFactory } from '@wizard/core';
import { useWizard, useWizardActions } from '@wizard/react';

export const MyWizard = createWizard(steps);

// Create typed hooks
export const useMyWizard = () => useWizard(MyWizard);
export const useMyWizardActions = () => useWizardActions<typeof MyWizard>();
```

Then use them in your components without extra typing:

```tsx
// Component.tsx
import { useMyWizard } from './wizard';

function MyComponent() {
  const { step } = useMyWizard(); // Fully typed!
}
```

## Router Integration

Be sure to check out the [Router Integration](/react/router-integration) guide to learn how to sync your wizard steps with the URL (TanStack Router, React Router, Next.js).

## API Reference

For detailed documentation of all components and hooks, see the [API Reference](/typedoc/react).