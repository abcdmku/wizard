# Zod Validation

Path: `examples/zod-validation`

Highlights:

- Zod schema validation per step
- typed validation context updates
- React step forms with `useWizardStep`

Run:

```bash
pnpm --filter @examples/zod-validation dev
pnpm --filter @examples/zod-validation build
```
