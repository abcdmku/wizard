# Examples

All examples in this section reflect the current API surface (`createWizardFactory` / `createReactWizardFactory`).

## Example Matrix

- `basic-form-wizard`
- `advanced-branching`
- `react-router-wizard`
- `node-saga-wizard`
- `persistence-local`
- `router-guard`
- `zod-validation`

## Build Validation

Docs checks run a build matrix against the in-scope examples via:

```bash
pnpm --filter @wizard/docs check:examples
```

The DAG visualizer demo is tracked separately from this matrix.
