# Persistence (Local Storage)

Path: `examples/persistence-local`

Highlights:

- localStorage save/load helpers
- persistence UX modes (instant/step/manual)
- React UI around a typed wizard config

Run:

```bash
pnpm --filter @examples/persistence-local dev
pnpm --filter @examples/persistence-local build
```
